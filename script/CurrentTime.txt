:CurrentTime
thread 'C_TIME'

:CurrentTimeStart
wait 0
00BF: $CHOUR = current_time_hours, $CMINUTE = current_time_minutes
0084: $CTIME = $CYEAR // integer values and handles
$CTIME *= 100 // integer values
0058: $CTIME += $CMONTH // integer values
$CTIME *= 100 // integer values
0058: $CTIME += $CDAY // integer values

0084: $CTIME1 = $CHOUR // integer values and handles
$CTIME1 *= 100 // integer values
0058: $CTIME1 += $CMINUTE // integer values
jump @CurrentTimeStart