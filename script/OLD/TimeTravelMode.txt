:Mode
thread 'MODE'

:ModeStart
wait 10
if and
    Player.Defined($PLAYER_CHAR)
    not Car.Wrecked($DELOREAN)
else_jump @ModeStart
if
00DC:   player $PLAYER_CHAR driving $DELOREAN
else_jump @ModeStart
if and
    $TRAVEL == 0 // integer values
    $CUTSCENE == 0 // integer values
    $MODE_CONTROL == 1 // integer values
    $CAMERA_CONTROL == 1 // integer values
    003A:   $KEY == $KEY_TOGGLEMODE // M
else_jump @ModeStart
$MODE += 1 // integer values
if
    $MODE == 1 // integer values
then
    03E5: text_box 'MODE1'  // Mode: Instant Time Travel Sequence
else
    $MODE = 0 // integer values
    03E5: text_box 'MODE2'  // Mode: Cutscene Time Travel Sequence
end
wait 1000
jump @ModeStart